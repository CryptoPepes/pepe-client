import React from "react";
import {withStyles} from "@material-ui/core/styles";

const styles = (theme) => ({

});


const CooldownInfo = (props) => {
    const {classes} = props;

    return (
        <span>
            TODO Cooldown pop-over with info
        </span>
    );
};

export default withStyles(styles)(CooldownInfo);
